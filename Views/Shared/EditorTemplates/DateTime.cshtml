@model DateTime?

@if (Model != null)
{
    @Html.TextBox("", Model.Value.ToString("dd.MM.yyyy"), new { @id = "date-picker", autocomplete="off" })
}
else
{
    @Html.TextBox("", "", new { @id = "date-picker", autocomplete = "off" })
}
